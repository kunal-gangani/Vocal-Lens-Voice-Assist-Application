import 'package:flutter/material.dart';

class DetailedResponsePages extends StatelessWidget {
  const DetailedResponsePages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}